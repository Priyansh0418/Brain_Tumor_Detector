flask>=2.0
pillow>=9.0
numpy>=1.23
requests>=2.28
h5py>=3.7
# scikit-learn is optional (used for evaluation/reporting). Install only if needed.
# scikit-learn>=1.0

# Notes:
# - TensorFlow is not included here because it is large and often requires a
#   specific Python/CUDA setup. If you need full Keras/TensorFlow inference,
#   install it from `backend/requirements-tf.txt`:
#       pip install -r backend/requirements-tf.txt
# - To create a venv and install these deps on Windows PowerShell:
#       py -3.11 -m venv .venv
#       .\.venv\Scripts\Activate.ps1
#       python -m pip install --upgrade pip setuptools wheel
#       pip install -r backend/requirements.txt
flask>=2.0
pillow>=9.0
numpy>=1.23
requests>=2.28
# Note: TensorFlow is optional and **not** required to run the server in dev mode
# (the server includes a DummyModel fallback when TensorFlow is unavailable).
# If you need real inference, install TensorFlow separately:
#   pip install -r backend\requirements-tf.txt
flask
tensorflow
pillow
numpy
werkzeug

# Note: tensorflow is large and may require specific system prerequisites (CUDA, etc.)
# If you only want to run the Flask server without loading a Keras model, you can
# omit tensorflow when installing.
